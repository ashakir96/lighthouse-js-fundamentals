const isCitizen = true;

const age = 26;

if (isCitizen && age > 18) {
    console.log("You are eligible to vote.")
}

const tempreture = -50

if (tempreture < -40 || tempreture > 40) {
    console.log("dont go outside");
}